package util

// interface转换为所需要的类型
func InterfaceChangeToType() {

	// var transRet []TaskInfo
	// for i := 0; i < len(ret); i++ {
	// 	task,ok:=ret[i].(TaskInfo)
	// 	if ok {
	// 		transRet = append(transRet, task)
	// 	}
	// }
	// result := []TaskInfo()
}
