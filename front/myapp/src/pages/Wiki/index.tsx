import { useEffect } from 'react';
const WikiList = () => {
  const getValue = (v) => {
    console.log(v);
  };

  useEffect(() => {}, []);

  return (
    <>
      <div></div>
    </>
  );
};

export default WikiList;
